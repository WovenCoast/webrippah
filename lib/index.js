/**
 * @type {import('./util')}
 */
const util = require('./util');
/**
 * @type {import('./cli')}
 */
const cli = require('./cli');
/**
 * @type {import('./http')}
 */
const http = require('./http');
/**
 * @type {import('./file')}
 */
const file = require('./file');

module.exports = { util, cli, http, file };